# Task List - 2026-02-27

## In Progress
- [ ] None

## Pending
- [ ] None

## Done Today
- [x] Implemented DB-backed services and route wiring in `main_app`.
- [x] Implemented end-to-end `face_service` pipeline (register, recognize, delete).
- [x] Installed `face_service` dependencies including `faiss-cpu`.
- [x] Ran API smoke tests for `face_service`.
- [x] Align `main_app` `face_client.py` payloads with face API contracts (JSON/base64 for recognize, multipart for register).
- [x] Integrate `main_app` `/v1/ops/upload-image` with `face_service` recognize/register endpoints.
- [x] Generate and apply `main_app` Alembic migrations from current SQLAlchemy models.
- [x] Add automated integration tests for `main_app` auth/admin/ops/user flows.
- [x] Add automated integration tests for `face_service` register/recognize/delete flows.
- [x] Persist `face_images` and `face_embeddings` metadata from face events into `main_app` DB tables.
- [x] Tighten DB model constraints to match `documentation/DB design.md` exactly.
- [x] Implement refresh token / logout / token revocation flow.
- [x] Remove weak fallback defaults (for example default user password path) and enforce explicit credential setup.
- [x] Add deployment-ready `.env` templates for dev/stage/prod.

## Notes
- Date reference is fixed to **2026-02-27**.
- Migration validation was executed with `DATABASE_URL=sqlite:///./alembic_test.db`.
- Automated integration tests were executed for both services.
